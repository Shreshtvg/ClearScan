{
  "name": "backend-server",
  "version": "1.0.0",
  "description": "Backend server for file processing",
  "main": "index.js",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.1",
    "multer": "^1.4.5-lts.1",
    "pdf-parse": "^1.1.1",
    "pdf2img": "^0.5.0",
    "sqlite": "^5.1.1",
    "sqlite3": "^5.1.7",
    "tesseract.js": "^5.1.1",
    "webextension-polyfill": "^0.12.0"
  },
  "devDependencies": {
    "eslint": "^9.15.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "scripts": {
    "start": "node server.js && ngrok http 5000",
    "dev": "nodemon server.js",
    "test": "jest server.test.js",
    "test:watch": "jest --watch",
    "lint": "eslint .",
    "build": "echo 'No build step'",
    "clean": "rm -rf node_modules && npm install"
  }
}
